<form method="post" action="{{ path('post_unreject', {'id': post.id}) }}" onsubmit="return confirm('¿Está seguro que desea aprobar esta publicación?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
    <button class="btn">Aprobar</button>
</form>
