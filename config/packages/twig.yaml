twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    exception_controller: null

    # Bundle View Paths
    paths:
        # Resources/Views
        '%kernel.project_dir%/src/Resources/views': views
